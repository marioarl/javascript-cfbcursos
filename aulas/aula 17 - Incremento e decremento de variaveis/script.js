var num=10;

num+=1; //Incremento de 1 na variavel num 
num-=1; //Decremento de 1 na variavel num
num*=2; //Multiplicando por 2 na variavel num
num++; // Forma simplificada de fazer o incremento de 1 na variavel num
num--; // Forma simplificada de fazer o decrento de 1 na variavel num



document.write(num, "<br>");
document.write(++num, "<br>") //pré incremento

//Inverter o valor de uma variavel de - para + e + para -
var num2=10;

//num2=num2*-1;
num2*= -1
document.write(num2)