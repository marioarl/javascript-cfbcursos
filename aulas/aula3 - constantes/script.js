const canal = "CFB Cursos"; //Declaracao de uma constante. Diferente de uma declaracao var que podemos alterar o seu conteudo a qualquer momento, o const não é possivel altera-la, pois uma vez definida terá o mesmo valor até o fim do script.
document.write(canal, "<br>")

//Existem varias const já definidas, exemplo a const pi.

document.write(Math.PI)